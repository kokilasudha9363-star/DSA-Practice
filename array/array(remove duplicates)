#include<iostream>
#include<vector>
using namespace std;
int main(){
    int n;
    cin>>n;
    vector<int>a(n);
    for(int i =0; i<n;i++){
        cin>>a[i];
    }
    vector<int>list;
    int i=0;
    for(int j=1;j<n;j++){
        if( a[j]!=a[i]){
            i++;
            a[i]=a[j];
            
        }}
     for(int k=0;k<=i;k++){
          cout<<a[k]<<" ";   
     }
   
    return 0;}